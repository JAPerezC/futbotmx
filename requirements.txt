# Stack pinneado verificado sobre Python 3.12.10 + RTX 5080 Laptop (16 GB VRAM)
# Fecha de validación: 2026-05-16
# Para reinstalar:
#   py -3.12 -m venv .venv
#   .venv/Scripts/pip install -r requirements.txt
#   .venv/Scripts/pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

# --- Visión por computadora núcleo ---
numpy==2.4.5
opencv-python==4.13.0.92
pillow==12.2.0

# --- Deep learning (torch viene con índice CUDA aparte, ver nota arriba) ---
# torch==2.11.0+cu128
# torchvision==0.26.0+cu128

# --- SAM 3.1 (pendiente instalación tras validación local) ---
# transformers>=4.45
# huggingface-hub>=0.24

# --- Tracking (pendiente) ---
# boxmot>=12.0       # incluye OC-SORT, ByteTrack, StrongSORT, DeepOCSORT
# supervision>=0.27  # Roboflow, integra SAM 3 + trackers

# --- Análisis y visualización (pendiente cuando se necesite) ---
# scipy>=1.11
# pandas>=2.2
# matplotlib>=3.8
# plotly>=5.20

# --- Utilidades ---
loguru==0.7.3

# --- Dev / tests ---
pytest==9.0.3
# ruff>=0.5       # opcional
# mypy>=1.10      # opcional
# ipykernel>=6.29 # cuando se agreguen notebooks
