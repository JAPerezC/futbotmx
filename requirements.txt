# Stack pinneado verificado sobre Python 3.12.10 + RTX 5080 Laptop (16 GB VRAM)
# Fecha de validación: 2026-05-16
# Para reinstalar:
#   py -3.12 -m venv .venv
#   .venv/Scripts/pip install -r requirements.txt
#   .venv/Scripts/pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

# --- Visión por computadora núcleo ---
numpy==2.4.5
opencv-python==4.13.0.92
pillow==12.2.0

# --- Deep learning (torch viene con índice CUDA aparte, ver nota arriba) ---
# torch==2.11.0+cu128
# torchvision==0.26.0+cu128

# --- SAM 3.1 (verificado 2026-05-16) ---
transformers==5.8.1
huggingface_hub==1.15.0

# --- Tracking (verificado 2026-05-16) ---
boxmot==18.0.0       # incluye OcSort, ByteTrack, StrongSort, DeepOcSort, BoTSort
supervision==0.28.0  # Roboflow, integra SAM 3 + trackers

# --- Computación científica ---
scipy==1.17.1

# --- Análisis y visualización (pendiente cuando se necesite) ---
# scipy>=1.11
# pandas>=2.2
# matplotlib>=3.8
# plotly>=5.20

# --- Utilidades ---
loguru==0.7.3
truststore>=0.9    # SSL via cert store del SO (necesario tras Norton/Kaspersky SSL inspection)
pyyaml>=6.0        # validación de goles (manual_goals.yaml)

# --- LLM (opcional, para commentary_pro.md) ---
anthropic==0.104.1  # SDK oficial Anthropic. Activado solo si ANTHROPIC_API_KEY presente.

# --- Dev / tests ---
pytest==9.0.3
# ruff>=0.5       # opcional
# mypy>=1.10      # opcional
# ipykernel>=6.29 # cuando se agreguen notebooks
